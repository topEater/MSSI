import numpy as np

# mask = np.random.binomial(1,0.25,(10000, 41, 41, 1))
# mask=mask.astype(np.float32)

# np.save("./data/mask-25.npy",mask)

# mask = np.random.binomial(1,0.30,(10000, 41, 41, 1))
# mask=mask.astype(np.float32)

# np.save("./data/mask-30.npy",mask)

# mask = np.random.binomial(1,0.40,(10000, 41, 41, 1))
# mask=mask.astype(np.float32)

# np.save("./data/mask-40.npy",mask)

# mask = np.random.binomial(1,0.50,(10000, 41, 41, 1))
# mask=mask.astype(np.float32)

# np.save("./data/mask-50.npy",mask)

# mask = np.random.binomial(1,0.60,(10000, 41, 41, 1))
# mask=mask.astype(np.float32)

# np.save("./data/mask-60.npy",mask)

# mask = np.random.binomial(1,0.70,(10000, 41, 41, 1))
# mask=mask.astype(np.float32)

# np.save("./data/mask-70.npy",mask)

# mask = np.random.binomial(1,0.75,(10000, 41, 41, 1))
# mask=mask.astype(np.float32)

# np.save("./data/mask-75.npy",mask)